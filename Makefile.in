.POSIX:
.SUFFIXES:

TARGET = @@TARGET@@
RM ?= rm -f

CC = @@CC@@
CFLAGS = @@CFLAGS@@ -Iinclude
LDFLAGS = @@LDFLAGS@@
INCLUDES = @@INCLUDES@@

default: noded
all: noded tools

tools: $(TOOLS)

include build.make

clean:
	$(RM) $(TARGET) $(TOOLS) $(TESTS) $(OBJECTS) $(OTHER_OBJS)
	rmdir build

.PHONY: default all tools test clean
